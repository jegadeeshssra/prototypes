Things I have learnt
    - To write "Custom handler"
    - Dependency Injection for log object(Handled globally)
    - To write "Custom HTTP server" with custom metrics like Idletimeout(to increase the time to wait for other reqs within the same established tcp connection) for performance tuning
    - On how to gracefully shutdown the server if receive any signals from OS


Concepts
    - Dependency Injection
    - HTTP Handler
    - DefaultServeMux
    - Timeouts(Idle,read,write)
    - Channels(Buffered Channel )
    - Signals
    - Context
